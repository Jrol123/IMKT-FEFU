#include "library.h"
#include <stdlib.h>
#include <stdio.h>

typedef struct Stack
{

} Stact_t;

struct Stract_t* init (int a, int max_size)
{
    int * ptr = (int*) malloc(max_size);

    return ptr;
}
